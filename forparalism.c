#include<stdio.h>
#include<omp.h>
int main()
{
	int arrB[] = {1,5,7,6,9};
	int n = arrB.length;
	int arrA[] = new int[n];
	for (int i = 1; i < n; i++) {
    		a[i] = a[i - 1] + b[i];
	}

	return 0;
}
